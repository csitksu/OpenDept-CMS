# Database URL: supports sqlite, postgresql, mysql
DATABASE_URL=sqlite:///./dev.db

# MySQL configuration (for docker-compose)
DB_ROOT_PASSWORD=root
DB_USER=opendept_user
DB_PASSWORD=opendept_password
DB_NAME=opendept_cms

